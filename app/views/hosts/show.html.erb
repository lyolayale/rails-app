<section class="text-center">
  <p style="color: green"><%= notice %></p>
  <%= render @host %>
  <div>
    <%= link_to "Edit this host", edit_host_path(@host), class: "text-secondary" %> |
    <%= link_to "Back to hosts", hosts_path %>
    <%= button_to "Destroy this host", @host, method: :delete, class: "btn btn-danger" %>
  </div>
</section>
